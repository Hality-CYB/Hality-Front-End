export default function RegistroPage() {
  return (
    <div className="text-center">
      <h1 className="text-lg">Registro</h1>
      <p className="text-muted-foreground mt-2 text-sm">
        Fica pra fase 2 — precisa capturar os dois consentimentos LGPD (RNF02) e o vínculo com
        profissional (RF05), não só um formulário simples.
      </p>
    </div>
  );
}
